# WireCellDocs (mirrors docs/wscript_build): no library/headers -- only docs.
# The org-mode -> HTML build and the styles/ install (gated on org2html) are
# handled by wct-ike.12; wct_package() here is a no-op placeholder for parity.
wct_package(WireCellDocs)
